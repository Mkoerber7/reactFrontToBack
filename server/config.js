// This is where we will put our apiKey so those nasty hackers don't steal our API keys.

// 9.
//	Set up a module.exports that will have an object containing your API key.
//	It doesn't matter what you name your key as the value is what actually matters.
module.exports = {
	apiKey: "421ac6ec71d27b9576b1d6f62f1254db"
};

// 10.
//	Great, now we need to add this file to the .gitignore file.
//  It's as simple as going to the .gitignore file in your root folder and adding
//	config.js to the file.

// Awesome, we've included our API key in a file that we can now hide from github!

// Head back to the control.js file for the next step.
